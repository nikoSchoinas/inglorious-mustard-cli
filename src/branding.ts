/**
 * Single source of truth for the product's name and slogan, so the banner
 * (ui/banner.ts) and the CLI `--help` description (cli.ts) can never drift apart.
 */
export const PRODUCT_NAME = 'Inglorious M.U.S.T.A.R.D.';
export const SLOGAN = 'Software specification for AI agents.';
